package com.lil_sf.booking.controller.mapper;

import com.lil_sf.booking.dto.HotelRegistrationDto;
import com.lil_sf.booking.models.Hotel;

public class HotelRegistrationMapper {
    /*
    public static Hotel mapAddressDtoToAddress(HotelRegistrationDto dto) {
        return Hotel.builder()
                .name(dto.getName())
                .description(dto.getDescription())
                .address(dto.getAddressDto())
    }

    public static AddressDto mapAddressToAddressDto(Address address) {
        return AddressDto.builder()
                //.id(address.getId())
                .line(address.getLine())
                .city(address.getCity())
                .country(address.getCountry())
                .build();
    }

     */
}
